#pragma once

#include <tuple>
#include <vector>
#include <string>
#include <algorithm>
#include <string>

bool DidSplit();
bool DidSort();
bool DidSort2();
std::vector<std::string> split(const std::string &str, char d);
